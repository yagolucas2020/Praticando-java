
public class inicio {
	public static void main(String[]Arg) {
		
		var nome = yago;
		var sobrenome = lucas;
		final var = nome + sobrenome;
		
		final var mensagem= String.format(s:"O cliente %$ possui sobre nome %$ ", nome + sobrenome);
	}
}
